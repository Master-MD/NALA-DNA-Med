<Page
    x:Class="NalaDnaMedWin.Views.BioLabPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="8">
            <TextBlock Text="BioLab Demo" FontSize="28" FontWeight="SemiBold" Foreground="#FF00FF88" />
            <Border Background="#20FF0000" BorderBrush="#80FF0000" BorderThickness="1" CornerRadius="4" Padding="8">
                <TextBlock Text="CAUTION: Simulated dry-run only. Not for clinical use." Foreground="#FFFF8888" FontWeight="Bold" />
            </Border>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0,16,0,16">
            <TextBlock x:Name="ResultTextBlock" Text="Enter a research query below to simulate a literature extraction." Foreground="#FFCCCCCC" TextWrapping="Wrap" />
        </ScrollViewer>

        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QueryTextBox" PlaceholderText="Search literature or compounds..." Grid.Column="0" Background="#11FFFFFF" Foreground="#FFFFFFFF"/>
            <Button Content="Simulate" Grid.Column="1" Background="#FF00FF88" Foreground="#FF000000" FontWeight="Bold" Click="Simulate_Click" />
        </Grid>
    </Grid>
</Page>
